---
- name: Test one
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: test task
      debug: msg="Test message from main.yml"
    - name: Local command
      command: hostname
  roles:
    - roles1
